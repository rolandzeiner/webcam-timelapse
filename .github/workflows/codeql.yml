name: CodeQL

# Scans Python (integration) + JavaScript/TypeScript (Lovelace card if any).
# Runs on every push to main/dev, every PR targeting main, and once a
# week on Tuesday at 04:23 UTC to catch newly-published advisories
# against code that hasn't been touched since the last scan.

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 2"

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
      packages: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: python
            build-mode: none
          - language: javascript-typescript
            build-mode: none

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          # paths-ignore keeps CodeQL focused on first-party source.
          # The built bundle is large + minified and triggers a flood
          # of false positives that the source TS already covers;
          # node_modules is third-party code we don't ship advisories
          # against. Drop the JS line if your integration has no card.
          config: |
            paths-ignore:
              - 'custom_components/webcam_timelapse/www/**/*.js'
              - 'node_modules/**'

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
