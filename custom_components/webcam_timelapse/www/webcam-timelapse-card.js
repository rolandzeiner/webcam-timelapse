/**
 * Webcam Timelapse Card — placeholder bundle (v0.1.0).
 *
 * The integration registers a Lovelace resource pointing at this file
 * during setup, so a missing file would 404 on every dashboard load. This
 * inert stub keeps that quiet until the real Lit card is built by
 * `npm run build`, which overwrites it.
 *
 * It deliberately does NOT define the custom element: a half-working
 * `webcam-timelapse-card` would be worse than an obviously absent one.
 */
console.info("%c WEBCAM-TIMELAPSE-CARD %c 0.1.0 (placeholder) ",
  "color:#fff;background:#3d7ea6;font-weight:700",
  "color:#3d7ea6;background:#fff;font-weight:700");
